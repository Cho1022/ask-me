services:
  backend:
    environment:
      GOOGLE_STT_ENABLED: "true"
      GOOGLE_APPLICATION_CREDENTIALS: /run/secrets/google-credentials.json
    volumes:
      - ${GOOGLE_CREDENTIALS_FILE:?GOOGLE_CREDENTIALS_FILE must point to a service-account JSON file}:/run/secrets/google-credentials.json:ro
